// ============================================================
//  dsh-aurora — 极光主题 · Host 半区(持久化版)
//  ============================================================
//  本插件是纯 Client 主题插件。这个 Host 半区只需要一个可激活的空 apply,
//  因为 loader 条目必须在 Host 侧激活成功(entry.fiber 存活),
//  dsh-client-modules 才会把它的 client bundle 纳入浏览器启动图
//  (window.__DSH_BOOT__) —— 没有 host 半区 = 条目加载失败 = 主题不生效。
//  主题的全部工作(theme.overrideTokens + 设置行)都在浏览器侧,
//  见同目录 client.js。
// ============================================================
export default {
  apply(ctx) {
    // 无事可做:主题完全在浏览器侧生效。
  },
}
